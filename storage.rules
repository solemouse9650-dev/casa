rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /homes/{homeId}/{allPaths=**} {
      allow read, write: if request.auth != null
        && (!('size' in request.resource) || request.resource.size < 10 * 1024 * 1024);
    }
  }
}
